{
  "name": "mongoose-mongdb-memory-server",
  "jest": {
    "testEnvironment": "node"
  },
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand ./test",
    "testOld": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^5.10.7"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "mongodb-memory-server": "^6.8.0"
  }
}
